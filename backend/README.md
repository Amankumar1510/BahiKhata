# to dos
1. enhance ai/command endpoint to handle all the operations.
2. Add more tools to ai/tools.py to handle other CRUD operations.
3. Current approach ai/command --> tools --> endpoints --> db.
   We can add more functionality like searching based on different different parameters like name, phone, transaction date.....etc.
4. Add one more file with code to handle the response from AI and update the db.
5. later on --> modify the response to handle both glaneable(to see) and listenable (to hear) options.